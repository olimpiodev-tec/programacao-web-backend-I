# Exemplo de algoritmo que converte temperatura
# De graus celsius em fahrenheit

temp_celsius = 27
temp_fah = temp_celsius * 1.8 + 32

print(f"A temperatura {temp_celsius} convertida em"
      f" fahrenheit é {temp_fah}")
